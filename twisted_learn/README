先修课 socket
开始进行twisted的学习，
第5章开始演示样例，3级，传输，协议，工厂。
工厂创建协议，协议由传输标识，twisted使用上层api实现回掉函数
注意产看twisted自带的协议，
Twisted内置了很多实现了通用协议的Protocol。
你可以在twisted.protocols.basic中找到一些稍微简单点的。



编写用于处理socket连接的最小系统，包括服务和客户端
1.客户端向服务端发送数据，服务端返回数据，客户端做数据记录。
用于模拟软件测试中的的数据收发性能测试。

emmm开始写用例

场景：客户端上传数据，服务器接收保存。
客户端：发送数据
服务端：监听，接收，处理，存储
数据格式：json
连接方式：tcp
注意：使用twisted实现的协议

嗯~ o(*￣▽￣*)o差不多了

