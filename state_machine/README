主要功能：
使用状态机从字符串中抽取数据帧

# 任意字符串中抽取数据包（字符串）返回值：（完整数据包组，7E开头的碎片）
(l1, l2)=TransitionState().Checkstring(s1)

前提：
通过协议来保证通信质量。
使用异步通信的时候，接收的数据帧会在任意位置中断。
帧中会有转义的情况。
帧中会丢失数据。
帧中会因不可预知的原因造成字节错误。

帧结构简述：
包头7E,报文长度字2字节,报文（长度自定义，与前报文长度字的值一致即可）,校验位1字节（计算方法：FF-报文段按字累加和）


详细说明：
使用状态机在任意的字符串中（包含数据包），抽取出"结构上"完整的数据帧，结构遵循“通信协议”。
注意！只是结构上完整，处理了转义的情况，未校验，即会存在字节传输错误的情况，该检测可通过校验位检查，在数据帧解析阶段实现。
返回的结果为两个list类型，会将参数中包含的所有结构完整的数据帧，和包头为7E的帧碎片返回。是为了方便调试。
实际使用时，碎片只有最后一个有保留的价值，可以与下个循环到达的数据拼接。
